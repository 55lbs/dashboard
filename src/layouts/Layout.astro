---
import Base from './Base.astro';

export type Props = {
  title: string;
  addToogles?: boolean;
};

const title = Astro.props.title;
const addToogles = Astro.props.addToogles ?? true;
---

<Base title={title} addToogles={addToogles}>
  <slot />
</Base>
